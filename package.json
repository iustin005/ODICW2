{
    "scripts": {
    	"heroku-prebuild": "cd backend && npm install",
    	"server": "cd backend && node server.js",
    	"heroku-postbuild": "cd frontend && npm install && npm run build && npm start"
    		
    },
    "engines": {
        "node": "14.16.1"
    }
}